package cc.baka9.catseedlogin.common.i18n;

/**
 * Type-safe message keys for i18n.
 * Each enum constant's name IS the message key.
 */
public enum MessageKey {

    // Login flow
    LOGIN_REQUEST,
    LOGIN_NOREGISTER,
    LOGIN_REPEAT,
    LOGIN_SUCCESS,
    LOGIN_FAIL,
    LOGIN_FAIL_IF_FORGET,
    REGISTER_REQUEST,

    // Registration
    REGISTER_SUCCESS,
    REGISTER_BEFORE_LOGIN_ALREADY,
    REGISTER_AFTER_LOGIN_ALREADY,
    REGISTER_PASSWORD_CONFIRM_FAIL,
    REGISTER_MORE,

    // Password reset
    RESETPASSWORD_NOREGISTER,
    RESETPASSWORD_EMAIL_DISABLE,
    RESETPASSWORD_EMAIL_NO_SET,
    RESETPASSWORD_EMAIL_REPEAT_SEND_MESSAGE,
    RESETPASSWORD_EMAIL_SENDING_MESSAGE,
    RESETPASSWORD_EMAIL_SENT_MESSAGE,
    RESETPASSWORD_EMAIL_WARN,
    RESETPASSWORD_SUCCESS,
    RESETPASSWORD_EMAILCODE_INCORRECT,
    RESETPASSWORD_FAIL,

    // Change password
    CHANGEPASSWORD_NOREGISTER,
    CHANGEPASSWORD_NOLOGIN,
    CHANGEPASSWORD_OLDPASSWORD_INCORRECT,
    CHANGEPASSWORD_PASSWORD_CONFIRM_FAIL,
    CHANGEPASSWORD_SUCCESS,

    // Common
    COMMON_PASSWORD_SO_SIMPLE,

    // System
    AUTO_KICK,
    BEDROCK_LOGIN_BYPASS,
    LOGIN_WITH_THE_SAME_IP,
    CONFIG_RELOADED,
    NO_PERMISSION,

    // Admin
    PLAYER_NOT_FOUND,
    PLAYER_DELETED,
    PASSWORD_SET,
    SPAWN_LOCATION_SET,
    IP_REG_COUNT_LIMIT_SET,
    IP_COUNT_LIMIT_SET,
    ID_LENGTH_SET,
    REENTER_INTERVAL_SET,
    AUTO_KICK_SET,
    FEATURE_TOGGLED,
    ADMIN_TOGGLE_ON,
    ADMIN_TOGGLE_OFF,
    ADMIN_AUTO_KICK_SET,
    ADMIN_AUTO_KICK_DISABLED,
    ADMIN_REENTER_INTERVAL_SET,
    ADMIN_ID_LENGTH_SET,
    ADMIN_IP_LOGIN_LIMIT_SET,
    ADMIN_IP_REG_LIMIT_SET,
    ADMIN_COMMAND_WHITELIST_INFO,
    ADMIN_COMMAND_WHITELIST_ADDED,
    ADMIN_COMMAND_WHITELIST_ALREADY_EXISTS,
    ADMIN_COMMAND_WHITELIST_REMOVED,
    ADMIN_COMMAND_WHITELIST_NOT_EXISTS,
    ADMIN_SET_FAILED,
    ADMIN_ENTER_NUMBER,

    // Errors
    CACHE_NOT_INITIALIZED,
    NAME_CASE_MISMATCH,
    PLAYER_ALREADY_ONLINE_ONLINE,
    TOO_MANY_SAME_IP,
    INVALID_NAME_PATTERN,
    NAME_TOO_SHORT,
    NAME_TOO_LONG,
    ILLEGAL_BEDROCK_NAME,
    INTERNAL_ERROR,
    DATABASE_ERROR,
    NOT_REGISTERED,
    NOT_LOGGED_IN,
    ERROR_PLEASE_RETRY,
    CHECK_PROXY_CONFIG,

    // Email
    EMAIL_ALREADY_BOUND,
    EMAIL_FORMAT_INVALID,
    EMAIL_CODE_ALREADY_SENT,
    SENDING_EMAIL_CODE,
    VERIFICATION_CODE_INCORRECT,
    BINDING_EMAIL,
    EMAIL_SENT_CHECK_INBOX,
    CHECK_SPAM_FOLDER,
    EMAIL_SEND_FAILED,
    EMAIL_BOUND_SUCCESS,
    NO_PENDING_EMAIL_CODE,

    // Other
    REGISTERING,
    CHANGING_PASSWORD,
    RESETTING_PASSWORD,
    SETTING_PASSWORD,
    CANNOT_USE_FROM_CONSOLE,
    SPAWN_LOCATION_SET_MSG,
    CONFIG_RELOADED_MSG,
    ACCOUNT_NOT_EXISTS,
    ACCOUNT_DELETED,
    ACCOUNT_DELETED_KICK,
    PASSWORD_TOO_SIMPLE_MSG,
    ACCOUNT_NOT_EXISTS_REGISTERED,
    PASSWORD_SET_MSG,
    PASSWORD_RESET_BY_ADMIN,
    ALREADY_LOGGED_IN_PROXY,

    // Email content
    EMAIL_SUBJECT_RESET_PASSWORD,
    EMAIL_SUBJECT_BIND_EMAIL,
    EMAIL_RESET_PASSWORD_CONTENT,
    EMAIL_BIND_EMAIL_CONTENT;

    /**
     * Get the message key string (same as enum name).
     */
    public String getKey() {
        return name();
    }

    /**
     * Get the localized message using the global I18n instance.
     */
    public String get(I18n i18n) {
        return i18n.get(name());
    }

    /**
     * Get the localized message with placeholders.
     *
     * @param i18n the I18n instance
     * @param placeholders alternating key-value pairs: "name", "Steve", "count", "5"
     */
    public String get(I18n i18n, String... placeholders) {
        return i18n.get(name(), placeholders);
    }
}
